> 使用说明：复制为 `<slug>-ui-spec.md`，与 `requirements/<slug>-prd.md` 共用同一 slug。详细章节说明见 `.cursor/skills/ui-designer-agent/SKILL.md`。**正文与表格说明默认简体中文**；可保留 `V-01`、`FR-01`、`P0`、路径等标识。设计原型为 **HTML**，放在 `design/prototypes/`（见下表「主原型」）。

# <功能标题> — UI 规格

| 字段 | 内容 |
|------|------|
| 状态 | 草稿 |
| Slug | `<slug>` |
| 对应 PRD | `docs/requirements/<slug>-prd.md` |
| 主原型 (HTML) | `docs/design/prototypes/<slug>-main.html` |
| 最后更新 | YYYY-MM-DD |

## 1. 设计范围与原则

## 2. 页面 / 视图清单

| 视图 ID | 名称 | 入口 | P0/P1 |
|--------|------|------|-------|

## 3. 逐屏规格

### V-01 <名称>

**布局（ASCII 或结构树）**

**组件与行为**

| 组件 | 类型 | 状态 | 行为 / 校验 | 备注 |
|------|------|------|-------------|------|

**文案与 i18n**

**数据与绑定（概念层）**

**无障碍与键盘**

## 4. 跨屏流程（序列）

## 5. 与 PRD 追溯

| 需求编号（FR） | 本规格章节 / 组件 |
|----------------|-------------------|

## 6. HTML 原型清单

- 主原型：`docs/design/prototypes/<slug>-main.html`
- 可选：`<slug>-dialog.html`、`<slug>-error.html` 等（同目录）

## 7. 设计假设与开放问题
